<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DailyReminder" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_variant</item>
        <item name="colorAccent">@color/secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary_variant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- High Contrast Theme -->
    <style name="Theme.DailyReminder.HighContrast" parent="Theme.DailyReminder">
        <item name="colorPrimary">@color/high_contrast_primary</item>
        <item name="colorAccent">@color/high_contrast_secondary</item>
        <item name="android:colorBackground">@color/high_contrast_background</item>
    </style>

    <!-- Large Text Theme -->
    <style name="Theme.DailyReminder.LargeText" parent="Theme.DailyReminder">
        <item name="android:textSize">18sp</item>
    </style>

    <!-- Text Appearances -->
    <style name="TextAppearance.DailyReminder.Headline1" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textSize">32sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="TextAppearance.DailyReminder.Headline2" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">24sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="TextAppearance.DailyReminder.Body1" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="TextAppearance.DailyReminder.Body2" parent="TextAppearance.AppCompat.Body2">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
</resources>